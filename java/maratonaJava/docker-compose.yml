version: '2.4'
services:
  database:
    image: postgres
    container_name: postgres
    environment:
      POSTGRES_PASSWORD: 1234
    ports:
      - "5433:5432"
    volumes:
      - devkaua_data:/var/lib/postgresql

volumes:
  devkaua_data: